# 复盘 - phase2_postmortem

## 问题现象

评测运行 `7` 暴露出误报与漏报案例，需要从规则覆盖、阈值设置和样本治理三方面复盘。

## 影响范围

- 运行名称：`phase2_ops_eval`
- 案例数：`28`

## 根因分析

['分类器泛化不足', '规则覆盖不足', '规则覆盖不足', '白样本与攻击模板局部重叠', '白样本与攻击模板局部重叠', '白样本与攻击模板局部重叠', '白样本与攻击模板局部重叠', '规则覆盖不足', '规则覆盖不足', '规则覆盖不足']

## 修复动作

1. 补充对应攻击类别样本。
2. 调整高误报规则的权重与场景约束。
3. 对分类器阈值做分场景细化。

## 后续跟踪项

1. 重新跑一轮对比评测。
2. 观察误报漏报案例是否下降。
3. 将修复经验沉淀进案例中心与周报。
